; Regs initialisation
    push 0
    pop rax
    push 0
    pop rbx
; Regs initialisation end

; Global array initialisation
; Global array initialisation end

; func call: main
    push rbx
    push rbx + 0
    pop rbx
    call ___func_1
    pop rbx
; func call end
hlt

; =========================== Function definition =========================
; func name: main
___func_1:
    push 123
    push 1
    push rbx
    add
    pop rcx
    push [rcx]
    ; swap last stk files
    pop rdx
    pop rex
    push rdx
    push rex

    div
    ; arr elem assignment: *the same*
    pop [rcx]

ret
; ------------------------- Function definition end -----------------------


